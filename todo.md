# Project cleanup todo

- [ ] `apps/desktop-app/src/main/settings/utils.ts` exports `clampPort`, but production code no longer imports it; only `apps/desktop-app/src/main/settings/utils.test.ts` covers it. This is dead fallback/clamping utility code and its dedicated tests can be removed unless a current endpoint editor path is meant to use it.
- [ ] `apps/desktop-app/src/main/fasterWhisperManager.ts:187` accepts `repoId` in `listModelFiles(repoId)`, but the method now returns the fixed `REQUIRED_MODEL_FILES` list and never reads the repo id. This looks like leftover scaffolding from a remote model-file listing path.
- [ ] `apps/desktop-app/src/main/jellyfinemby/JellyfinembyConnection.ts:21` has a `disposed` field that is only assigned in `connect`/`dispose` and never read; the actual disposed guard lives in `JellyfinembyWebSocketTransport`.
- [ ] `apps/desktop-app/src/main/mediaServer/MediaServerMessageHandler.ts:15` creates a `mediaserver-message-handler` logger that is never used.
- [ ] `apps/desktop-app/src/main/stateManager.ts:317` accepts `next: AppSettings` in `handleSettingsUpdated(previous, next)`, but the method only reads `previous` and current state.
- [ ] `apps/desktop-app/src/main/transcriptionService.ts:297` and `apps/desktop-app/src/main/transcriptionService.ts:328` pass `config` into `buildTrackFromJson` / `buildTrackFromText`, but both methods ignore it. The matching test-only type declarations in `apps/desktop-app/src/main/transcriptionService.test.ts` keep the unused parameter alive.
- [ ] `apps/desktop-app/src/main/window/windowController.ts:58` has an `isQuitting` field that is assigned during quit paths but never read, so it is a stale lifecycle guard.
- [ ] `apps/desktop-app/src/main/settings/constants.ts:14` imports `DEFAULT_TRANSCRIPTION_YTDLP_ARGS` only to re-export the same symbol again from `../../common/transcriptionDefaults.js`; the local import is redundant.
- [ ] `apps/desktop-app/src/main/fasterWhisperManager.test.ts:48` names a mocked `command` parameter that the test never reads.
- [ ] `apps/desktop-app/src/main/jellyfinemby/types.ts:5` imports `MediaServerConfig` but never uses it in the Jellyfin/Emby type aliases.
- [ ] `apps/extension/src/video/ControlHandler.ts:2` imports `send` but never uses it.
- [ ] `apps/extension/src/video/VideoDetector.ts:7` imports `BackgroundToContentMessage` but never uses it.
- [ ] `apps/extension/src/video/VideoStateGatherer.ts:4` imports `PlaybackPrediction` but never uses it.
- [ ] `apps/desktop-app/src/main/logger.ts:316` exports the root `logger` singleton and `apps/desktop-app/src/main/logger.ts:318` exports `setLogLevel`, but current code only imports `createLogger`. These are unused public surface on the logger module.
- [ ] `apps/desktop-app/src/common/transcriptionDefaults.ts` exports `BASE_TRANSCRIPTION_CONFIG` and `DefaultTranscriptionConfigOptions`, but both are only used inside the same module through exported factory signatures. They can be made internal unless another package is intended to consume them.
- [ ] `apps/desktop-app/src/common/wordLookupDefaults.ts:3` exports `DEFAULT_WORD_LOOKUP_PANEL_SIZE`, but only the same file reads it to build `DEFAULT_WORD_LOOKUP_PLUGIN_CONFIG`.
- [ ] `apps/desktop-app/src/main/window/wordLookupWindowManager.ts:38` exports `WordLookupWindowPayload`, while the renderer defines its own identical local payload type in `apps/desktop-app/src/renderer/components/subtitle/WordLookupWindow.vue`. The main-process export is not imported anywhere.
- [ ] `apps/desktop-app/src/main/plugins/official/jellyfinemby/index.ts` is an unused barrel that only re-exports `manifest` and `registerMain`; active code imports those files directly.
- [ ] `apps/desktop-app/src/main/jellyfinemby/index.ts` is a broad barrel used only for `JellyfinembySubtitleService` imports in media-server modules, but it re-exports constants, identity helpers, event types, session manager, subtitle loader, connection, and service. This overexposes internals and can be narrowed or removed in favor of direct imports.
- [ ] `apps/extension/src/background/endpoints/EndpointManager.ts:66` carries a `fallbackToDefault` option that is only used during background startup and one unit test. It converts an empty endpoint list back to the default endpoint, which is a fallback branch rather than a single final endpoint-list behavior.
- [ ] `apps/desktop-app/src/main/plugins/pluginHost.ts:29` deletes unknown plugin records from the registry and `apps/desktop-app/src/main/settings/appSettingsSanitizer.ts:156` rejects unknown plugin settings. Since only bundled official plugins exist today and the product has not shipped, this looks like an old/foreign plugin-record rejection layer that may be removable together with its tests.
- [ ] `apps/desktop-app/src/main/settings/appSettingsSanitizer.test.ts:181` checks that disk-loaded settings do not expose a historical `mediaServer` top-level field, and `apps/desktop-app/src/main/settings/appSettingsSanitizer.test.ts:187` checks that old `network.host` / `network.port` fields are absent. These are old-shape rejection tests rather than current behavior tests.
- [ ] `apps/desktop-app/src/main/settings/appSettingsSanitizer.ts:95` plus the sanitizer tests around `defaultProfileId` (`apps/desktop-app/src/main/settings/appSettingsSanitizer.test.ts:24`, `:30`, `:53`, `:86`) keep a disk-load path that ignores saved default-profile ids, creates missing fallback profiles, and reorders fallback profiles. If no legacy settings need migration, this is a compatibility/defaulting layer to simplify.
- [ ] `apps/desktop-app/src/main/settings/sanitizers/jellyfinembySanitizer.ts:22` prefixes `webSocketPath` with `/` on disk load, while `validateJellyfinembyPluginConfigForUpdate` rejects the same non-current value. That split is a read-time compatibility normalization path.
- [ ] `apps/desktop-app/src/main/settings/sanitizers/transcriptionSanitizer.ts:34` converts arbitrary `extraParams` values to strings on disk load, while update validation rejects non-string values. This is another legacy/malformed-settings coercion path.
- [ ] `apps/desktop-app/src/main/settings/sanitizers/profileSanitizer.ts:48` and `apps/desktop-app/src/main/settings/sanitizers/cacheSanitizer.ts:30` clamp/default malformed persisted values, while their update validators reject those values. The project now has two settings semantics: permissive disk-load repair and strict write-time validation.
- [ ] `apps/desktop-app/src/main/mediaServer/MediaServerUrlResolver.ts:95` falls back to the first media-server config when no config id is supplied and no config is enabled. With explicit `enabled` flags in the current settings shape, selecting a disabled server is a questionable fallback.
- [ ] `apps/desktop-app/src/main/mediaServer/MediaServerUrlResolver.ts:124` returns a synthetic `jellyfinemby://<itemId>` URL when no server base URL resolves. Current consumers expect normal app/page/video URLs, so this placeholder scheme looks like an unnecessary fallback path.
- [ ] The workspace contains ignored generated artifacts: `apps/desktop-app/dist/`, `apps/desktop-app/out/`, `apps/extension/dist/`, `packages/contracts/dist/`, `packages/plugin-sdk/dist/`, `apps/desktop-app/.vitest-attachments/`, `apps/desktop-app/.vitest-traces/`, and `apps/desktop-app/electron-fuses-1.8.0.tgz`. They are not tracked, but they pollute project-wide scans and should be cleaned from the working tree when auditing.
- [ ] `apps/extension/src/popup.ts:87` exports `appearanceLabel`, but no code calls or imports it.
- [ ] `apps/extension/src/shared/reportError.ts:6` exports `reportError`, but extension code only imports `swallow` from this module.
- [ ] `apps/desktop-app/src/renderer/style.css:1542`, `:1841`, and `:2292` define `.settings-field--wide`, `.transcription-config-list__label`, `.cache-field-row`, `.cache-field--grow`, and `.cache-field--fixed`, but no renderer template or script references those class names. These look like leftover CSS from earlier settings/cache layouts.
- [ ] `apps/extension/src/shared/icons.ts:77` and `apps/extension/src/shared/icons.ts:81` export `createAddIcon` and `createDeleteIcon`, but production extension code only imports `createArrowLeftIcon` and `createCloseIcon`; the unused icon factories are kept alive only by `apps/extension/src/shared/icons.test.ts`.
- [ ] `apps/desktop-app/src/main/plugins/official/wordLookup/wordListValidator.ts:62` accepts JSON arrays, `{ entries: [...] }`, `{ words: [...] }`, and single `{ word, content }` objects, while `apps/desktop-app/src/renderer/components/settings/SettingsWordLookup.vue:3` documents JSONL rows as the mounted word-list format. If JSONL is the final format, the alternate shapes and their tests in `apps/desktop-app/src/main/plugins/official/wordLookup/wordLookup.test.ts:54`, `:68`, and `:86` are import/compatibility paths.
- [ ] `apps/desktop-app/src/main/plugins/official/wordLookup/wordListCli.ts:62` normalizes with `{ skipInvalid: true }`, silently dropping bad rows. For a not-yet-shipped project with a final JSONL format, this is a forgiving cleanup path rather than strict validation.
- [ ] `apps/desktop-app/src/main/plugins/official/wordLookup/wordListParser.ts:6` and `apps/desktop-app/src/main/plugins/official/wordLookup/wordListValidator.ts:22` contain duplicated `parseAliases` logic with the same trimming/deduping behavior.
- [ ] `apps/desktop-app/src/main/plugins/official/wordLookup/WordLookupService.ts:134` keeps the previous loaded word index after a later refresh fails, and `apps/desktop-app/src/main/plugins/official/wordLookup/wordLookup.test.ts:127` locks that fallback in. If the final behavior should reflect the current configured file only, this stale-index fallback can be removed.
- [ ] `apps/desktop-app/src/main/settings/sanitizers/jellyfinembySanitizer.ts:93` converts `JellyfinembyPluginConfig` into `MediaServerSettings`, and `apps/desktop-app/src/main/types.ts:113` still carries `MediaServerSettings` / `MediaServerConfig.type`. Since the current persisted shape is the Jellyfin/Emby plugin config, this looks like a transitional host media-server model.
- [ ] `apps/desktop-app/src/main/plugins/official/jellyfinemby/registerMain.ts:13` wraps `MediaServerController.activate()` in a plugin contribution with an empty `commands` object, so the plugin layer is only an enable/disable indirection for a built-in controller.
- [ ] `packages/plugin-sdk/src/manifest.ts` is a private workspace package containing only plugin manifest/contribution types, but it adds a separate package build and dependency path for code that is only consumed inside `apps/desktop-app`. If external plugin distribution is not part of the pre-launch target, this SDK package is an over-layer.
- [ ] `apps/desktop-app/src/main/ipc/handlers/pluginHandlers.ts:22`, `:30`, and `:45` return synthetic empty/error results when built-in Word Lookup commands are missing, and `apps/desktop-app/src/main/ipc/handlers/transcriptionHandlers.ts:6` does the same for transcription. If official plugins are final built-ins, these disabled-plugin fallbacks can be replaced with direct service calls or explicit errors.
- [ ] `apps/desktop-app/src/main/i18n.ts:26` and `apps/desktop-app/src/renderer/i18n.ts:25` duplicate language normalization and named-placeholder interpolation logic. The renderer needs async dictionary loading, but the pure `normalizeLanguage` / replacement formatting behavior could be shared or kept in one module.
- [ ] `apps/desktop-app/src/main/fasterWhisperManager.ts:68` and `apps/desktop-app/src/renderer/components/settings/transcription/composables/useFasterWhisper.ts:58` duplicate the same `faster-whisper-` prefix stripping helper.
- [ ] `apps/desktop-app/src/renderer/components/settings/transcription/composables/useTranscriptionConfig.ts:47` repeats the `extraParams` object/key/string validation that `apps/desktop-app/src/main/settings/sanitizers/transcriptionSanitizer.ts:85` already enforces on settings updates. This creates a second validation pipeline in the renderer.
- [ ] `apps/desktop-app/src/renderer/components/settings/transcription/composables/useTranscriptionConfig.ts:268` silently replaces non-finite VAD threshold input with the current value, while `apps/desktop-app/src/main/settings/sanitizers/transcriptionSanitizer.ts:118` rejects non-finite values. This is another renderer-side fallback around the strict main-process validator.
- [ ] `apps/desktop-app/src/renderer/stores/desktop/actions/profileActions.ts:7` and `apps/desktop-app/src/main/settings/sanitizers/profileSanitizer.ts:282` both implement “fallback profile stays last” ordering. The main update validator already rejects invalid profile order, so the renderer helper is a duplicate normalization layer.
- [ ] `apps/desktop-app/src/main/jellyfinembyUtils.ts:23` returns a trimmed string for unparsable server URLs instead of rejecting them; `apps/desktop-app/src/main/jellyfinembyUtils.test.ts:49` explicitly tests this fallback. Current settings validation should make unparsable server URLs impossible to persist.
- [ ] `apps/extension/src/monitoring/URLWatcher.ts:43` keeps a 10-second polling fallback on top of patched `pushState`, `replaceState`, `popstate`, and `hashchange` events. If supported sites are covered by the event-driven path, this periodic fallback can be removed.
- [ ] `scripts/check-silent-catches.mjs` only bans truly empty catch blocks; it does not flag catch blocks that convert errors into fallback values or stale state. That leaves the project with a “no silent catch” check while many try/catch fallback paths remain outside the audit rule.
- [ ] `apps/desktop-app/src/main/settings/SettingsStore.ts:24` catches any settings load failure and resets the entire settings object to defaults. With no shipped legacy settings to rescue, this broad fallback can hide malformed current settings instead of surfacing a hard failure.
- [ ] `docs/superpowers/` contains 23 tracked implementation plan/spec files (13,086 lines, about 488 KB), including old no-compatibility planning notes. If these are agent working artifacts rather than product documentation, they are historical project clutter and can be removed or moved outside the source tree.
- [ ] `apps/desktop-app/src/renderer/components/settings/SettingsProfiles.browser.test.ts` is 1,483 lines and includes historical assertions such as “instead of a separate editor field” (`:328`), “without set-default controls” (`:1030`), and “without enable switches or row actions” (`:1059`). These tests preserve old-design rejection language rather than just asserting final UI behavior.
- [ ] `apps/desktop-app/src/main/connectionAuth.ts:37` exports `isTrustedExtensionOrigin`, but it is only called inside `isAuthorizedDesktopClient` in the same file.
- [ ] `apps/desktop-app/src/main/subtitleParser.ts:210` exports `parseTimestamp`, but only `apps/desktop-app/src/main/subtitleParser.ts` itself calls it.
- [ ] `apps/desktop-app/src/renderer/i18n.ts:70` exports `formatTranslation`, but current renderer code only reaches it through `useI18n` inside the same module.
- [ ] `apps/desktop-app/src/renderer/theme.ts:9` exports `getSystemPrefersDark`, but it is only used internally by `resolveTheme` / `useDocumentTheme`.
- [ ] `apps/extension/src/background/tabs/MediaStateStore.ts:5` exports `MINIMUM_DURATION`, but it is only used as the constructor default in the same file.
- [ ] `apps/desktop-app/src/main/settings/constants.ts` re-exports `DEFAULT_SUBTITLE_PRIMARY_COLOR`, `DEFAULT_SUBTITLE_SECONDARY_COLOR`, `DEFAULT_SUBTITLE_ACTIVE_PRIMARY_COLOR`, and `DEFAULT_SUBTITLE_ACTIVE_SECONDARY_COLOR`, but those names are only consumed inside `apps/desktop-app/src/common/defaultSettings.ts` and the re-export surface is unused.
- [ ] `apps/desktop-app/src/main/settings/constants.ts:44` exports `DEFAULT_LANGUAGE`, and `apps/desktop-app/src/main/settings/constants.ts:51` exports `DEFAULT_FASTER_WHISPER_MODEL_DIR`; both are only used in the same settings constants module.
- [ ] `apps/desktop-app/src/main/settings/sanitizers/jellyfinembySanitizer.ts:16`, `apps/desktop-app/src/main/settings/sanitizers/profileSanitizer.ts:48`, `:198`, `:272`, and `apps/desktop-app/src/main/settings/sanitizers/transcriptionSanitizer.ts:34` export sanitizer helper functions that are only called inside their own sanitizer modules.
- [ ] `apps/desktop-app/src/main/settings/sanitizers/networkSanitizer.ts:18` exports `NetworkSettingsValidationError`, but callers never branch on this specific class; it can be internal or replaced with `Error`.
- [ ] `apps/desktop-app/src/renderer/components/ui/UiSelect.vue:65`, `apps/desktop-app/src/renderer/utils/errorBus.ts:1`, `:7`, `apps/desktop-app/src/renderer/stores/desktop/types.ts:25`, `:36`, and `:48` export component/store helper types that are only used in their defining modules.
- [ ] `apps/desktop-app/src/main/plugins/official/jellyfinemby/registerMain.ts:3` exports `JellyfinembyPluginController`, but the interface is only needed by `JellyfinembyPluginContext` in the same file.
- [ ] `apps/desktop-app/src/main/plugins/pluginTypes.ts:3` exports `LocalPluginStatus`, but it is only used to type `PluginCatalogRow.status` in the same file.
- [ ] `apps/extension/src/monitoring/DOMObserver.ts:17` exports `ensureDocListeners` and `apps/extension/src/monitoring/DOMObserver.ts:39` exports `scanForShadowRoots`, but both are internal implementation details of DOM monitoring.
- [ ] `apps/desktop-app/src/main/settings/index.ts:2` re-exports `sanitizeSettings`, `mergeSettings`, and `DEFAULT_SETTINGS_FACTORY`; production imports use `SettingsStore` / constants through this barrel, while sanitizer tests and `SettingsStore` import the sanitizer module directly.
- [ ] `apps/desktop-app/package.json:19` runs `tsx` for `word-list:normalize`, but `tsx` is not declared in `apps/desktop-app` devDependencies; it is currently available only because another workspace package brings it in. If the CLI remains, the dependency should be explicit, otherwise the script is another cleanup candidate.
