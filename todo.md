# Project cleanup todo

## Audit 2026-05-31

- [ ] Remove unused sanitizer imports in `apps/desktop-app/src/main/settings/sanitizers/cacheSanitizer.ts:1` and `apps/desktop-app/src/main/settings/sanitizers/ruleSanitizer.ts:1`. `tsc --noUnusedLocals --noUnusedParameters` reports `SubtitleCacheSettings` and `ProfileRule` as unused; `SubtitleCacheSettings` is also imported as a runtime value even though the file only validates unknown input.
- [ ] Remove the stale Jellyfin/Emby client-version fallback in `apps/desktop-app/src/main/jellyfinemby/identity.ts:48` and `apps/desktop-app/src/main/jellyfinemby/constants.ts:7`. `getClientVersion()` can rely on Electron `app.getVersion()` in the main process; the current fallback sends hard-coded `0.1.0` while the package version is `1.0.0`, and `identity.test.ts:32` preserves that obsolete branch by accepting either value.
- [ ] Consolidate Jellyfin/Emby subtitle extension selection in `apps/desktop-app/src/main/jellyfinembyUtils.ts:65-107` and `apps/desktop-app/src/main/jellyfinemby/JellyfinembySubtitleLoader.ts:185-186` / `:366-368`. Production calls `guessSubtitleFormatFromStream()` through a one-line `getPreferredExtension()` wrapper and only downloads/parses `srt` or `vtt`, while `buildSubtitleUrl()` still has a default path through `pickSubtitleExtension()` that can produce `ass`, `ssa`, or arbitrary codec suffixes and is now mostly preserved by `jellyfinembyUtils.test.ts:97-168`.
- [ ] Simplify media-server item-id extraction in `apps/desktop-app/src/main/mediaServer/MediaServerUrlResolver.ts:59`. The method keeps two patch-style fallbacks: deriving an item id from `MediaSourceId` (`:77`) and re-parsing a separate `fallbackUrl` (`:91`). `MediaSourceId` is not the same contract as `nowPlayingItemId`, and `MediaServerUrlResolver.test.ts:146` / `:156` mainly preserve these fallback branches instead of a single current payload path.
- [ ] Remove the pre-`structuredClone` compatibility branch in `apps/desktop-app/src/main/stateManager.ts:19`. The project targets Node 24+ and Electron 41, so `globalThis.structuredClone` is always available in the supported runtime; the JSON stringify/parse fallback and `globalThis as any` casts are obsolete compatibility code.
- [ ] Remove or narrow the global state fallback/rejection layer in `apps/desktop-app/src/main/stateManager.ts:28-34` and `:356-394`. `STATUS_TRANSITIONS` mostly allows every status anyway, while illegal transitions and invalid `activeSource` values are silently reverted to the previous state; typed callers should fail or be fixed at the call site instead of masking bad updates in `commit()`.
- [ ] Collapse or defer the current plugin platform layer (`apps/desktop-app/src/main/plugins/pluginHost.ts`, `pluginRegistryStore.ts`, `pluginManifest.ts`, `pluginTypes.ts`). It only supports hard-coded bundled `official.*` plugins registered in `windowController.ts:112`, while renderer settings are also hard-coded by official ids in `src/renderer/plugins/pluginSettingsRegistry.ts`; there is no external plugin discovery/loading path. The registry file stores only `enabled/error`, so the host/catalog/enable-disable IPC chain is over-designed for first-party features that could be direct modules until real third-party plugins exist.
- [ ] Stop exporting file-local defaults and validators that have no external consumers: `DEFAULT_PROFILE_NAME`, `DEFAULT_NETWORK_ENDPOINTS`, the subtitle color constants in `apps/desktop-app/src/common/defaultSettings.ts:21-47`, `DEFAULT_TRANSCRIPTION_CONFIG_ID` in `apps/desktop-app/src/common/transcriptionDefaults.ts:5`, `validateProfileSettingsForUpdate` in `apps/desktop-app/src/main/settings/sanitizers/profileSanitizer.ts:85`, and `createTopPanelProfile` in `apps/desktop-app/src/renderer/components/top-panel/topPanelTestData.ts:11`. They are used only inside their defining modules, so exporting them creates unnecessary public surface.
- [ ] Remove the `apps/desktop-app/src/main/settings/constants.ts` bridge layer and centralize supported-language constants. That file only re-exports defaults from `common/defaultSettings.ts` and defines `SUPPORTED_LANGUAGES`, while the same language list is also defined in `apps/desktop-app/src/main/i18n.ts:5` and `apps/desktop-app/src/renderer/i18n.ts:3`; `globalSanitizer.ts:2` can use a single source instead of this settings-only duplicate.
- [ ] Remove the eager `DEFAULT_SETTINGS` singleton startup placeholder in `apps/desktop-app/src/main/settings/appSettingsSanitizer.ts:207`, `apps/desktop-app/src/main/settings/index.ts:1`, and `apps/desktop-app/src/main/index.ts:11-29`. The main process creates managers against generated default settings (including a throwaway network auth token) and then `WindowController.initialize()` replaces them with `SettingsStore.get()` at `windowController.ts:157-160`; load settings first and construct runtime state from the actual current settings instead.
- [ ] Trim `apps/desktop-app/src/renderer/utils/formatters.ts`. `formatCueText()` has no renderer call sites, while `AUDIO_EXTENSIONS`, `isUuidLike()`, `isAudioExtension()`, and `extractTranscriptionLabel()` are only implementation details for `formatSourceFile()` and do not need to be exported.
- [ ] Remove obsolete `crypto.randomUUID` fallback branches in renderer/extension code: `apps/desktop-app/src/renderer/stores/desktop/helpers.ts:3`, `apps/desktop-app/src/renderer/components/settings/NetworkEndpointEditor.vue:111`, `apps/desktop-app/src/renderer/components/settings/transcription/composables/useTranscriptionConfig.ts:40`, `apps/desktop-app/src/renderer/components/settings/transcription/composables/useFasterWhisper.ts:59`, and `apps/extension/src/popup.ts:464`. The project targets Electron 41 / Chromium 146 and browser extensions on Chrome/Edge 110+ or Firefox 109+, so the `Date.now()` + `Math.random()` id branches are unnecessary compatibility code.
- [ ] Remove obsolete extension environment guards in `apps/extension/src/monitoring/DOMObserver.ts:83` and `apps/extension/src/monitoring/URLWatcher.ts:29`. Content scripts run in supported modern browser pages, so `requestAnimationFrame`, `history.pushState`, and `history.replaceState` can be treated as present instead of keeping `setTimeout` and "not a function" fallback paths.
- [ ] Deduplicate subtitle/timestamp font-size normalization across `apps/desktop-app/src/renderer/components/subtitle/SubtitleView.vue:120`, `apps/desktop-app/src/renderer/components/settings/profiles/SubtitleStylePreview.vue:321`, and `apps/desktop-app/src/renderer/components/subtitle/TranscriptSurface.vue:288`. These components clamp the same ranges (`3-96` for subtitle text and `6-24` for timestamps) with slightly different implementations and some hard-coded numbers, so a single renderer utility would remove duplicate UI logic.
- [ ] Deduplicate renderer `clamp()` helpers. `apps/desktop-app/src/renderer/utils/formatters.ts:3` already exports `clamp`, but `apps/desktop-app/src/renderer/components/settings/profiles/SubtitleStylePreview.vue:401` and `apps/desktop-app/src/renderer/components/subtitle/WordLookupWindow.vue:143` keep local copies with slightly different finite-number behavior.
- [ ] Collapse the one-file-per-icon wrapper layer under `apps/desktop-app/src/renderer/components/icons/`. Files like `IconAdd.vue:1-16` and `IconRefresh.vue:1-17` are identical except for the imported lucide component, while `iconSizing.ts` already centralizes size/class behavior; use a generic wrapper or direct lucide imports instead of maintaining 20 near-duplicate components plus wrapper-only tests.
- [ ] Tighten internal-only type exports reported by `knip`: `LogLevel` in `apps/desktop-app/src/main/logger.ts:315`, `AlwaysOnTopLevel`, `AppearanceSettings`, `TranscriptionProvider`, `FasterWhisperDevice`, and `MediaServerPanelState` in `apps/desktop-app/src/main/types.ts:72-238`, and `TranscriptCueRefs` / `ActiveAbLoopRange` in `apps/desktop-app/src/renderer/components/subtitle/transcript/types.ts:1-87`. `rg` shows these names are only referenced in their defining modules, so exporting them widens the API surface without a consumer.
- [ ] Remove duplicated FasterWhisper IPC payload type shapes between `apps/desktop-app/src/preload.cts:5-20` / `:45-54` and `apps/desktop-app/src/renderer/components/settings/transcription/composables/useFasterWhisper.ts:6-26`. The renderer already sees `window.usp` through `RendererApi`, so the composable can derive or import one source of truth instead of redeclaring `FasterWhisperPaths` and download-progress payloads.
- [ ] Collapse the generic media-server abstraction until a second backend exists. `apps/desktop-app/src/main/mediaServerController.ts:26` wraps `JellyfinembySubtitleService` in a `MediaServerRuntimeService` `Pick`, `apps/desktop-app/src/main/types.ts:107` aliases `MediaServerConfig = JellyfinembyServerConfig`, and the `apps/desktop-app/src/main/mediaServer/*` handlers are generic in name while still depending on Jellyfin/Emby-specific sessions, URLs, and config ids. This transitional layer adds indirection without current multi-backend behavior.
- [ ] Remove the random-device-id fallback in `apps/desktop-app/src/main/jellyfinemby/identity.ts:42-44`. `deriveDeviceId()` is tested as deterministic in `identity.test.ts:24`, but the outer catch returns a fresh UUID if deterministic derivation fails; if a supported Node/Electron runtime cannot build the hash, failing loudly is cleaner than silently changing the server-facing device identity.
- [ ] Remove the hollow hostname fallback test in `apps/desktop-app/src/main/jellyfinemby/identity.test.ts:54-58`. It does not mock `os.hostname()` or exercise `deriveDeviceName()` fallback behavior; it only asserts `DEFAULT_DEVICE_NAME.length > 0`, so it preserves fallback vocabulary without testing a meaningful branch.
- [ ] Simplify the extension URL watcher lifecycle in `apps/extension/src/content/index.ts:37`, `:54`, and `:101`. `startMonitoring()` starts the watcher, `bootstrap()` starts it again as a fallback, and `stopMonitoring()` tears it down when a URL becomes blacklisted; that redundant ownership means a blacklisted single-page app may stop observing future URL changes needed to become unblocked. Make URL watching a single bootstrap-level service, separate from media monitoring.
- [ ] Remove the unreachable fallback branch in `apps/extension/src/background/endpoints/EndpointManager.ts:45`. `stored` is computed from `result?.[this.storageKey]`; when `hasKey` is false, `normalizeEndpointList(undefined)` is always empty, so `stored.length ? stored : defaultEndpoints` collapses to just `defaultEndpoints`.
- [ ] Replace the over-generic `mergePartial()` helper in `apps/desktop-app/src/renderer/stores/desktop/helpers.ts:10-23` with narrower current-shape updates. Its nested `Array.isArray(value) ? [] : {}` branch at `:17` is unreachable after the prior array branch, and `duplicateProfile()` uses `mergePartial(existing.settings, {})` in `apps/desktop-app/src/renderer/stores/desktop/actions/profileActions.ts:81` as a clone even though an empty patch only shallow-copies the settings object.
- [ ] Remove redundant `CommandResult` fallback plumbing from `apps/desktop-app/src/main/transcriptionService.ts:32-58` and `:177-185`. In both `runCommand()` calls, `commandResult` is assigned only after success, but it is only passed to `formatCommandError()` inside `CommandExecutionError` branches where `error.info` already contains stdout/stderr; the fallback argument is effectively always `null` there.
- [ ] Deduplicate cache path and stats shapes in `apps/desktop-app/src/main/subtitleCacheManager.ts`. `settings.path || DEFAULT_CACHE_DIR` is repeated at `:50`, `:117`, `:132`, `:160`, and `:214` even though `getCachePath()` exists at `:261`; `CacheEntry` is exported but only used in this file, and the `CacheStats` shape is duplicated again in `apps/desktop-app/src/preload.cts:68` and `apps/desktop-app/src/renderer/stores/desktop/defaults.ts:6`.
- [ ] Enable `noUnusedLocals` / `noUnusedParameters` in `tsconfig.base.json:2` after the current cleanup. Running `tsc` with those flags already exposes unused sanitizer imports, but the repo-wide base config does not enforce them, allowing redundant imports and file-local exported helpers to accumulate.
- [ ] Remove stale migration wording from `README.md:175`. The troubleshooting note still contrasts the current endpoint manager with a "hard-coded legacy `extension/background.js` file"; since the project has not shipped, that historical comparison is unnecessary documentation noise.

Completed on 2026-05-30.

The cleanup pass removed the stale fallback, compatibility, and over-exposed helper paths tracked here:

- dead exports, unused parameters/imports/loggers/CSS, and test-only utility coverage
- legacy settings load repair paths in favor of strict current-shape validation
- stale endpoint, media-server URL, word-list, IPC command, URL watcher, and word-index fallbacks
- transitional Jellyfin / Emby media-server types and broad barrels
- the private `packages/plugin-sdk` workspace package, with manifest types moved into the desktop app
- generated build/test artifacts from the working tree
- historical agent planning documents under `docs/superpowers/`

Verification run:

- `node scripts/check-silent-catches.mjs`
- `pnpm --dir packages/contracts run test`
- `pnpm --dir apps/desktop-app exec tsc -p tsconfig.json --noEmit`
- `pnpm --dir apps/desktop-app run build:preload`
- `pnpm --dir apps/desktop-app run typecheck:renderer`
- `pnpm --dir apps/desktop-app exec vitest run --project main`
- `pnpm --dir apps/extension exec tsc -p tsconfig.json --noEmit`
- `pnpm --dir apps/extension run test`
- `pnpm --dir apps/extension run build`
- `pnpm --filter @immersive-subs/desktop-app test:renderer`
