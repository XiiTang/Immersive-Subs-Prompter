# 清理审查待办

范围：本轮只做审查记录，不改运行时代码。该项目尚未上线，不需要兼容旧数据或旧实现；以下清单用于后续删除过度设计、冗余兜底、无用测试、过时文档、过渡层和重复链路。

- [ ] 删除旧设置清洗器的历史保护测试。项目尚未上线，但 `apps/desktop-app/src/main/settings/appSettingsSanitizer.test.ts:61`、`apps/desktop-app/src/main/settings/SettingsStore.test.ts:119`、`apps/desktop-app/src/main/settings/SettingsStore.test.ts:151`、`apps/desktop-app/src/main/settings/SettingsStore.test.ts:341`、`apps/desktop-app/src/main/settings/SettingsStore.test.ts:390`、`apps/desktop-app/src/main/settings/sanitizers/networkSanitizer.test.ts:20` 仍在保护“旧磁盘读取清洗器会 normalize/ignore/default 的值现在应拒绝”的行为。保留当前结构校验测试即可，删除这些旧行为兼容测试。
- [ ] 让语言字典成为 UI 文案的唯一来源，移除调用点英文兜底。非测试代码中有 285 处 `t()/translate()/formatMessage()` 调用重复传入英文 fallback，例如 `apps/desktop-app/src/renderer/components/settings/SettingsGlobal.vue:25`、`apps/extension/src/popup.ts:159`；这些英文已经存在于 `apps/desktop-app/src/renderer/locales/en.json` 和 `apps/extension/_locales/en/messages.json`。fallback 参数还让 `apps/desktop-app/src/renderer/i18n.ts:53`、`apps/extension/src/shared/i18n.ts:64` 更复杂，并让 renderer 保留 `apps/desktop-app/src/renderer/i18n.ts:19` 的 `localeRevision` 与懒加载兜底。预上线阶段应缺 key 就显式暴露，或同步加载默认字典。
- [ ] 删除扩展端未使用的语言包 key 及对应测试期望。`popupNoMediaInitialDescription` 和 `statusError` 存在于 `apps/extension/_locales/en/messages.json:29`、`apps/extension/_locales/en/messages.json:98`、`apps/extension/_locales/zh/messages.json:29`、`apps/extension/_locales/zh/messages.json:98`，但生产代码没有引用；`statusError` 只被 `apps/extension/src/i18n.test.ts:32` 保活。删除这些死 key，或先接入真实 UI。
- [ ] 删除或重写过时的部署文档。`DEPLOYMENT.md:12`、`DEPLOYMENT.md:33`、`DEPLOYMENT.md:46` 仍写着 `cd extension` / `cd desktop-app` 与 `npm ci`，但当前仓库使用 `apps/extension`、`apps/desktop-app`、pnpm workspace 和根目录 filter 脚本，README 的实际入口在 `README.md:72`。`DEPLOYMENT.md:52` 还链接不存在的 `desktop-app/forge.config.mjs`，`DEPLOYMENT.md:76` 提到 `WS_ENDPOINT` / `USP_WS_PORT`，而当前网络端点已经由设置页和扩展端存储管理。
- [ ] 简化 `TabRegistry` 中冗余的防御分支。`apps/extension/src/background/tabs/TabRegistry.ts:14` 的 `ensureTabInfo()` 写入 map 后立即重新读取，又在 `apps/extension/src/background/tabs/TabRegistry.ts:19` 保留不可达的 `if (!existing)` 兜底；`clearFrame()` 在 `apps/extension/src/background/tabs/TabRegistry.ts:86` 也先处理了 `!frames` 后仍检查 `frames && frames.size === 0`。可直接简化，不改变行为。
- [ ] 删除或集中 `connectionLabel` 这个未使用的 store getter。`apps/desktop-app/src/renderer/stores/desktop.ts:53` 计算硬编码英文连接状态，但真实 UI 在 `apps/desktop-app/src/renderer/components/top-panel/TopControlPanel.vue:257` 本地计算并走 i18n。直接引用只出现在 `apps/desktop-app/src/renderer/stores/desktop.test.ts:407`，属于死 store API 或重复展示逻辑。
- [ ] 删除只被测试使用的生产导出。`formatNetworkEndpointInput()` 从 `packages/contracts/src/core/network-endpoints.ts:27` 导出并由 `packages/contracts/src/index.ts:2` 重新导出，但非测试搜索只找到定义；`ticksToSeconds()` 从 `apps/desktop-app/src/main/jellyfinembyUtils.ts:57` 导出，但生产代码使用毫秒转换。下列同文件 helper 也只为测试导出，应改为私有或通过公共入口测试：`apps/desktop-app/src/main/jellyfinembyUtils.ts:65`、`apps/desktop-app/src/main/jellyfinembyUtils.ts:116`、`apps/desktop-app/src/main/window/wordLookupWindowManager.ts:83`、`apps/desktop-app/src/renderer/theme.ts:15`、`apps/desktop-app/src/renderer/theme.ts:21`、`apps/desktop-app/src/renderer/theme.ts:26`、`apps/desktop-app/src/main/jellyfinemby/identity.ts:17`、`apps/desktop-app/src/main/jellyfinemby/identity.ts:29`、`apps/desktop-app/src/main/jellyfinemby/identity.ts:36`、`apps/desktop-app/src/renderer/i18n.ts:31`、`apps/extension/src/shared/i18n.ts:38`、`apps/extension/src/shared/i18n.ts:51`、`apps/desktop-app/src/renderer/components/subtitle/transcript/translate.ts:5`。
- [ ] 将 top-panel 测试 fixture 移出生产组件树或内联到测试。`apps/desktop-app/src/renderer/components/top-panel/topPanelTestData.ts:39`、`apps/desktop-app/src/renderer/components/top-panel/topPanelTestData.ts:69`、`apps/desktop-app/src/renderer/components/top-panel/topPanelTestData.ts:125` 只导出测试数据 helper，唯一导入方是 `TopControlPanel.test.ts` / `TopControlPanel.browser.test.ts`。放在 `src/renderer/components` 会让生产树看起来比实际更大。
- [ ] 将扩展端 type-only 测试移出生产源码目录。`apps/extension/src/connection/MessageSender.type-test.ts:1` 是带 `@ts-expect-error` 的编译期断言文件，没有运行时消费者；它只是因为 `apps/extension/tsconfig.json:14` 覆盖 `src/**/*.ts` 才参与检查。应移动到测试/类型测试目录，或建立专门的 type-test 脚本，让 `src/connection` 只放生产模块。
- [ ] 合并 Jellyfin/Emby 字幕状态的重复 reset 方法。`apps/desktop-app/src/main/jellyfinemby/JellyfinembySubtitleLoader.ts:41` 的 `resetState()` 与 `apps/desktop-app/src/main/jellyfinemby/JellyfinembySubtitleLoader.ts:48` 的 `clearSubtitleState()` 都递增 `subtitleRequestToken` 并清空同三个字段。调用点分布在 dispose 和会话/字幕变化处：`apps/desktop-app/src/main/jellyfinemby/JellyfinembyConnection.ts:68`、`apps/desktop-app/src/main/jellyfinemby/JellyfinembyConnection.ts:114`、`apps/desktop-app/src/main/jellyfinemby/JellyfinembyConnection.ts:199`、`apps/desktop-app/src/main/jellyfinemby/JellyfinembyConnection.ts:208`。保留一个方法即可。
- [ ] 收紧或删除空 catch 的文件级白名单。`scripts/check-silent-catches.mjs:39` 只要文件包含 `usp-allow-empty-catch` 就跳过整个文件，而 `README.md:166` 说豁免注释应位于具体 `catch` 上一行。这会让一个允许的空 catch 遮住同文件后续所有空 catch。要么统一要求 `swallow(...)`，要么让检查器只豁免相邻 catch 块。
- [ ] 删除项目树里的本地 Vitest/Playwright trace 产物。`apps/desktop-app/.vitest-traces/` 当前有 2340 个生成的 `.trace` / `.network` 文件，且 `.gitignore:128` 已将 `.vitest-traces/` 视为测试运行产物。它们不是源码、fixture 或文档化审查产物，会干扰全项目搜索。
- [ ] 重写关于网络范围的过时商店/隐私声明。`PRIVACY_POLICY.md:25`、`PRIVACY_POLICY.md:36` 写着只通过 `ws://127.0.0.1:44501` 与 localhost 通信，`STORE_LISTING.md:71` 写着没有外部服务器通信；但当前扩展 popup 可添加任意 `ws://host:port` 端点（`apps/extension/src/popup.ts:180`、`packages/contracts/src/core/network-endpoints.ts:60`），桌面端也可调用配置的 Whisper-compatible API（`apps/desktop-app/src/main/transcriptionService.ts:135`）。上线前要么移除远程端点/云转写能力，要么更新文档。
- [ ] 重写 Chrome Web Store 支持站点声明。`STORE_LISTING.md:22` 和 `STORE_LISTING.md:53` 宣传 Netflix，但当前 README 主要描述 YouTube、Bilibili、Douyin、Jellyfin/Emby（`README.md:10`、`README.md:23`），实现也依赖桌面 companion、`yt-dlp` 或媒体服务器链路，而不是 Netflix 专用路径。上线前需让商店文案与真实支持/测试站点一致。
- [ ] 删除过时的 Jellyfin/Emby URL fallback 测试。`apps/desktop-app/src/main/jellyfinemby/JellyfinembyUrlResolver.test.ts:163` 名为“不从 separate fallback url 推断 item id”，但 `JellyfinembyUrlResolver.extractItemId()` 只读取 `payload.videoSrc`（`apps/desktop-app/src/main/jellyfinemby/JellyfinembyUrlResolver.ts:59`），该测试也只传空 `videoSrc`。它现在只是 `apps/desktop-app/src/main/jellyfinemby/JellyfinembyUrlResolver.test.ts:170` 旁边的重复 null-path 断言。
- [ ] 内联过薄的 Jellyfin/Emby tab context registry。`apps/desktop-app/src/main/jellyfinemby/JellyfinembyTabContextRegistry.ts:7` 只是用一个 `Map<number, JellyfinembyTabContext>` 包了 `get`、`update`、`clear`、`clearAll`、`entries`；生产消费者都在相邻媒体服务器控制器/handler 中：`apps/desktop-app/src/main/mediaServerController.ts:29`、`apps/desktop-app/src/main/jellyfinemby/JellyfinembyMessageHandler.ts:15`、`apps/desktop-app/src/main/jellyfinemby/JellyfinembySessionHandler.ts:14`、`apps/desktop-app/src/main/jellyfinemby/JellyfinembyStatusHandler.ts:17`。这个类没有隔离独立领域逻辑，可以并入控制器或拥有上下文决策的 handler。
- [ ] 删除无人调用的 Pinia store actions。`updateProfileMeta()` 定义在 `apps/desktop-app/src/renderer/stores/desktop/actions/profileActions.ts:38`，通过 `apps/desktop-app/src/renderer/stores/desktop/actions/profileActions.ts:191` 导出，并在 `apps/desktop-app/src/renderer/stores/desktop/types.ts:69` 声明，但没有生产或测试调用。`updateRule()` 同样定义/导出/声明于 `apps/desktop-app/src/renderer/stores/desktop/actions/ruleActions.ts:15`、`apps/desktop-app/src/renderer/stores/desktop/actions/ruleActions.ts:65`、`apps/desktop-app/src/renderer/stores/desktop/types.ts:85`。删除 action、类型面和只为保留 API 的测试。
- [ ] 删除未接 UI 的 cache clear/cleanup 桥接链路，或补上真实 UI。主进程在 `apps/desktop-app/src/main/ipc/handlers/cacheHandlers.ts:10`、`apps/desktop-app/src/main/ipc/handlers/cacheHandlers.ts:15` 注册 `usp:cache-clear` / `usp:cache-cleanup`；preload 在 `apps/desktop-app/src/preload.cts:71`、`apps/desktop-app/src/preload.cts:72` 暴露；renderer store 在 `apps/desktop-app/src/renderer/stores/desktop/actions/cacheActions.ts:10`、`apps/desktop-app/src/renderer/stores/desktop/actions/cacheActions.ts:16` 暴露，类型在 `apps/desktop-app/src/renderer/stores/desktop/types.ts:101`、`apps/desktop-app/src/renderer/stores/desktop/types.ts:102`。当前 UI 只刷新 stats 和打开文件夹，这条 clear/cleanup API 链路是死链。
- [ ] 删除被直接 state 更新取代的 `StateManager` setter。`setActiveSource()` 和 `setActiveTab()` 定义于 `apps/desktop-app/src/main/stateManager.ts:124`、`apps/desktop-app/src/main/stateManager.ts:130`，但仓库搜索没有生产或测试调用。其他代码已通过 `updateState()` / `setPageContext()` 设置这些字段。
- [ ] 删除 renderer profile 初始化中的冗余兜底。`AppSettings.defaultProfileId` 在类型 `apps/desktop-app/src/main/types.ts:202` 和加载校验 `apps/desktop-app/src/main/settings/appSettingsSanitizer.ts:66`、`apps/desktop-app/src/main/settings/appSettingsSanitizer.ts:73` 中都是必填且固定，但 `initialize()` 和 settings-change listener 仍在 `apps/desktop-app/src/renderer/stores/desktop/actions/initActions.ts:11`、`apps/desktop-app/src/renderer/stores/desktop/actions/initActions.ts:36` 退到 `settings.profiles[0]` / `null`。应删除不可达 fallback，让设置边界负责暴露 invariant 问题。
- [ ] 删除 current-shape 校验之后的全局设置冗余兜底。`global.language`、`global.gameProcessBlacklist`、`global.toggleWindowShortcut` 由 `apps/desktop-app/src/main/settings/appSettingsSanitizer.ts:25`、`apps/desktop-app/src/main/settings/appSettingsSanitizer.ts:87`、`apps/desktop-app/src/main/settings/sanitizers/globalSanitizer.ts:5` 要求存在，但生产代码仍在 `apps/desktop-app/src/main/window/windowController.ts:66`、`apps/desktop-app/src/main/window/windowController.ts:97`、`apps/desktop-app/src/main/window/windowController.ts:249`、`apps/desktop-app/src/renderer/stores/desktop/actions/gameBlacklistActions.ts:8`、`apps/desktop-app/src/renderer/stores/desktop/actions/gameBlacklistActions.ts:20` 回退到 `"en"`、`[]` 或 `""`。保留 settings 未加载 guard，删除已由 sanitizer 保证的字段兜底。
- [ ] 删除工作树中的 ignored 本地构建输出。`dist/` 是生成产物且已被忽略，但当前工作区仍有 158 个构建文件分布在 desktop、extension、contracts 包中。这些文件不是源码，可能与当前源码漂移，也会干扰全项目审查。参考：`apps/desktop-app/dist/`、`apps/extension/dist/`、`packages/contracts/dist/`、`.gitignore:7`。
- [ ] 去重生成/静态 logo 资源。`assets/logo.png`、`apps/desktop-app/src/renderer/logo.png`、`apps/extension/icons/icon-128.png` 的 SHA-256 完全相同，而且 `apps/extension/icons/icon-128.png` 实际是 512x512 PNG，文件名/manifest key 却写 128。应保留一个源资源，在构建时生成或复制 favicon/扩展尺寸，避免漂移。参考：`README.md:5`、`apps/desktop-app/src/renderer/index.html:8`、`apps/extension/manifest.json:36`、`apps/extension/manifest.firefox.json:36`。
- [ ] 删除或说明过时的 desktop `.npmignore`。桌面端通过 Electron Forge 分发，实际打包排除规则在 `apps/desktop-app/forge.config.mjs:30`；仓库搜索没有发现会使用 `apps/desktop-app/.npmignore` 的 `npm pack` / `npm publish` 流程。一个非发布 npm 包保留第二套打包排除列表，会形成死的兼容面。参考：`apps/desktop-app/.npmignore:1`、`apps/desktop-app/package.json:23`、`apps/desktop-app/forge.config.mjs:30`。
- [ ] 将 desktop typecheck 与 build 输出拆开。`apps/desktop-app/package.json:31` 的 `typecheck:app` 实际调用 `build:main`、`build:preload`、`typecheck:renderer`；而 `build:main` / `build:preload` 在 `apps/desktop-app/package.json:10`、`apps/desktop-app/package.json:11` 运行不带 `--noEmit` 的 `tsc`，导致一次 typecheck 就会生成 `apps/desktop-app/dist/`。应增加 main/preload 的 no-emit typecheck 脚本，把 `dist/` 生成留给显式 build/package。
