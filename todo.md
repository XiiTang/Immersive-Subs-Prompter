# Source Review TODO

- [x] `apps/desktop-app/src/main/settings/SettingsStore.ts` + `apps/desktop-app/src/main/settings/appSettingsSanitizer.ts`: 设置文件读取时，JSON 解析失败或当前结构校验失败会直接返回 `DEFAULT_SETTINGS_FACTORY()`，并且 `SettingsStore.test.ts` 还固定了“损坏 settings.json 自动加载默认值”的行为。项目尚未上线时这属于不必要的兜底/兼容路径，会隐藏配置写入 bug 和 schema 错误；建议改为让损坏或不符合当前结构的设置显式报错，只保留“文件不存在时创建当前默认设置”这一条初始化路径。
- [x] `apps/desktop-app/src/main/subtitleCacheManager.test.ts`: 过期缓存测试通过 `retentionDays: -1` 制造“立刻过期”，但 `apps/desktop-app/src/main/settings/sanitizers/cacheSanitizer.ts` 明确拒绝小于 1 的 retentionDays。该测试覆盖的是运行时不会出现的非法状态，属于冗余/误导性的兜底测试；建议改为直接写入旧 timestamp 的缓存文件，或通过可注入时钟测试真实合法配置下的过期逻辑。
- [x] `apps/desktop-app/src/renderer/components/subtitle/transcript/types.ts` + `pretextLayout.ts` + `projectTranscript*.test.ts`: 当前视口/窗口投影只使用 `blocks[].top/height/blockId`，但 `TranscriptLayoutBlock` 仍生产并测试 `lineStart/lineCount`，测试里还保留了 `TranscriptLayoutResult.lines: []` 这种类型中已不存在的旧字段。这是 line 级布局阶段遗留的无用元数据和过时测试夹具；建议删除 `lineStart/lineCount` 及相关断言，同时清理测试里的 `lines` 字段。
- [x] `apps/desktop-app/src/renderer/components/settings/SettingsGlobal.vue` + `apps/desktop-app/src/renderer/locales/*.json`: locale 中定义了 `cache-open-error`，但 `openCacheFolder()` 只是直接调用 `store.openCacheFolder()`，没有 `await/catch`、错误状态或 toast 分支，导致这条错误文案和错误展示链路实际不可达。建议要么实现明确的错误展示，要么删除未使用的 locale key，避免留下半截兜底逻辑。
- [x] `apps/desktop-app/src/renderer/locales/en.json` + `apps/desktop-app/src/renderer/locales/zh.json`: `settings-back`、`global-shortcut-exclusions`、`global-autohide`、`global-network`、`global-shortcuts`、`toggle-enable`、`auto-hide-hint`、`auto-hide-panels-label`、`language-hint`、`subtitle-typography-group`、`subtitle-layout-group`、`subtitle-behavior-group`、`rule-match-label`、`rule-pattern-label`、`rule-delete-confirm`、`default-badge`、`rule-action-enable`、`game-blacklist-none`、`alert-delete-default-profile`、`alert-delete-last-profile`、`pin-label-unpinned`、`pin-label-floating`、`pin-label-screensaver`、`primary-track-placeholder`、`secondary-track-placeholder`、`secondary-track-label` 在非插件源码中没有任何调用点；当前 UI 已改用新的 section/panel/track key，profile 删除限制也只是 `console.warn`。这些是旧 UI/旧提示残留的无用本地化项；建议直接删除，或恢复真实 UI 调用后再保留。
- [x] `apps/desktop-app/src/main/subtitleParser.ts` + `apps/desktop-app/src/main/subtitleParser.test.ts`: `parseSubtitle()` 只在 `extension === "srt"` 时走 SRT，其他任何扩展名都会静默按 VTT 解析，测试还固定了 `"unknown"` 默认 VTT 的行为。当前 ytdlp 调用方已经只筛选 `.srt/.vtt` 文件，这个未知扩展名 fallback 会隐藏调用方传错格式的问题；建议改成只接受当前支持的 `srt | vtt`，未知格式直接抛错或返回明确错误，并删除该 fallback 测试。
- [x] `apps/desktop-app/src/main/stateManager.test.ts`: 测试夹具多处把 `defaultProfileId` 改成 `"profile-default"`，但 `apps/desktop-app/src/common/defaultSettings.ts` 固定当前默认配置 ID 为 `"default-profile"`，`appSettingsSanitizer.ts` 也拒绝任何其他 `defaultProfileId`。这些测试覆盖的是运行时不会出现的非法设置状态，容易让 `StateManager` 继续背负“任意默认 profile id”的兼容假设；建议改用真实 `DEFAULT_PROFILE_ID`，只调整 profile 顺序和规则来覆盖匹配逻辑。
- [x] `apps/desktop-app/src/renderer/stores/desktop.test.ts` + `apps/desktop-app/src/renderer/components/settings/SettingsProfiles.browser.test.ts` + `apps/desktop-app/src/renderer/components/settings/profiles/ProfileList.test.ts`: renderer 测试夹具同样大量使用 `"profile-default"` 作为 `defaultProfileId`，并直接写入 Pinia store，绕过了当前 `"default-profile"` 的固定默认配置约束。这会让 profile 排序、删除、URL 规则和 fallback profile UI 测试继续运行在非法设置形状上；建议统一复用真实 `DEFAULT_PROFILE_ID`/默认 profile 工厂，或建立一个当前结构的轻量 test factory。
