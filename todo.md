# Project Review TODO

- [ ] 精简内置插件系统的过度抽象：当前只有固定的内置功能（`official.transcription`、`official.word-lookup`、`official.jellyfinemby`），但实现了 `PluginHost`、`PluginRegistryStore`、manifest、catalog、enable/disable、command dispatch、renderer plugin registry 等完整插件平台链路。项目未上线且没有外部插件加载需求时，这层未来扩展架构会让核心功能多走 registry 状态、字符串命令和动态组件映射；建议改成显式模块/设置分区，删除 `apps/desktop-app/src/main/plugins/pluginHost.ts`、`pluginRegistryStore.ts` 及对应 enable/disable/catalog 测试和 IPC 过渡层，保留真正需要的功能入口。
- [ ] 删除或大幅收缩设置 schema 的“旧结构拒绝层”：`apps/desktop-app/src/main/settings/appSettingsSanitizer.ts`、`sanitizers/*`、`SettingsStore.test.ts`、`appSettingsSanitizer.test.ts` 组合出约 1500 行手写校验和测试，且大量断言是“rejects ... instead of sanitizing/defaulting”“must use the current ... setting”“unknown plugin/settings rejected”。项目未上线且不需要迁移旧 settings.json 时，加载旧/坏配置可以直接重置默认或让当前 UI 写入合法结构；保留 IPC 边界必要校验即可，移除针对历史字段、未知插件记录、固定 fallback profile 顺序等旧数据拒绝测试。
- [ ] 收敛 Faster-Whisper 转写的厂商/平台预留面：`fasterWhisperManager.ts` 同时维护 Windows-only CPU/GPU 托管下载、`Faster-Whisper-XXL` 7z 解压、HuggingFace/hf-mirror/ModelScope 三源模型下载 fallback、`fasterWhisperUseKim2` 专用参数，以及 renderer 端下载进度/状态 UI。若当前目标只是先跑通转写，建议先保留一种明确支持路径（例如手动配置本地 binary+model，或只保留 Whisper API），删除 GPU/Kim2/多镜像下载兜底及相关设置字段、IPC、测试，等真正要发版本地模型管理时再补。
- [ ] 合并 Jellyfin/Emby 会话与字幕链路的过细分层：`mediaServerController.ts` 再分出 `JellyfinembyConnection`、`SessionSubscription`、`SessionTracker`、`SessionManager`、`SubtitleLoader`、`MessageHandler`、`SessionHandler`、`StatusHandler`、`UrlResolver` 等对象，同时维护 extension video context、tab context、selected session、pending item、burst/continuous/idle session polling、session 元数据缺失后再请求 `/Items/{id}` 的 fallback。项目未上线时建议先保留一个明确路径（已配置 server -> 当前 session -> 拉字幕 -> 更新状态），删除连续轮询/突发轮询状态机、tab 级 session 记忆和二次 metadata 兜底，等确认真实 Jellyfin/Emby 客户端差异后再补兼容。
- [ ] 简化字幕缓存子系统：`SubtitleCacheManager` 现在同时提供内存缓存、磁盘 JSON 缓存、访问时刷新 timestamp、启动后 30 秒清理、每 12 小时清理、损坏文件吞错删除、统计接口、打开缓存目录 IPC、设置页开关/路径/保留天数/统计展示，并被 ytdlp、media server、transcription 三条链路共用。未上线阶段建议先保留最小的单层磁盘缓存或直接关闭缓存默认链路，删除 `getStats`/统计 UI/周期清理 timer/内存层刷新写盘等治理代码，避免缓存策略先于真实使用量固化。
- [ ] 收敛桌面端与扩展端的多网络端点配置：桌面端 `ConnectionManager` 支持多个 WebSocket listener、listener 状态、非 loopback token 授权、动态重启；设置页 `NetworkEndpointEditor` 可编辑多个端点；扩展端 `EndpointManager`、`DesktopConnectionPool` 和 popup 又维护一份 `serverEndpoints` 存储、连接状态和增删 UI。项目未上线且默认只需要本机扩展连接时，建议固定 `127.0.0.1:44501` 单端点，删除多端点编辑器、扩展端 endpoint storage/dashboard 消息、远程 token URL 生成和对应测试；等确实要支持局域网/多实例再恢复。
- [ ] 删除 renderer 设置保存的 deferred/rollback 状态机：`stores/desktop/actions/settingsActions.ts` 为滑块和颜色输入维护 `WeakMap` 队列、revision、optimistic merge、rollback、`flushDeferredSettingsPersistence` 和测试；实际使用点只在 `SubtitleStyleFields.vue`、`ColorSchemeGrid.vue` 的高频外观设置。项目未上线时可以改成组件本地草稿 + `change/blur` 时一次 `updateSettings`，或简单 debounce 单函数；删除全局 deferred persistence、revision 防竞态和相关测试，降低 settings store 的隐式行为。
- [ ] 暂停或缩小划词查询的独立插件/浮窗实现：`official.word-lookup` 现在包含 JSONL 解析、exact/case/normalized/alias 六套索引、状态刷新、设置页、IPC command、`WordLookupWindowManager` 独立透明 BrowserWindow、指针 handoff 关闭 timer、自定义 Markdown renderer、自定义滚动条和 resize 持久化。该功能不在 README 主链路中，未上线阶段建议先改成主字幕窗口内的简单内嵌弹层（或暂时移除插件），删除独立窗口、手写 Markdown 子集、panelSize 持久化、复杂索引分层和对应测试。
- [ ] 清理“反历史 UI”测试：`SettingsGlobal.test.ts`、`SettingsWindowShell.test.ts`、`SettingsProfiles.browser.test.ts`、`SettingsMediaServer.test.ts`、`SettingsPlugins.test.ts`、`TopControlPanel.test.ts`、`PillListEditor.test.ts` 等包含大量 `not.toContain(...)`、`exists() === false`、不存在旧按钮/旧文案/旧布局的断言（例如不再显示 `Global Settings`、`Add URL Rule`、`Install`、旧 host/port 输入、旧编辑按钮等）。这些更像补丁后的防回归清单，不是当前产品行为契约；建议保留关键交互测试，删除纯粹证明旧 UI 已消失的断言，减少重构阻力。
- [ ] 缩小 URL 规则语言：`packages/contracts/src/core/url-rule-matcher.ts` 同时支持 domain、glob、`=` exact、`re:` regex、`contains:` 五种匹配，并在 desktop profile rules 与 extension blacklist 中复用；UI 还为这些类型做 label、校验和测试。未上线阶段建议只保留 domain/contains 或一套明确语法，删除 glob/exact/regex 前缀、规则类型标签、跨端 blacklist 复用测试，等用户确实需要高级 URL 匹配再扩展。
- [ ] 移除或延后“快捷键进程黑名单”：`GameProcessMonitor` 依赖 `get-windows` 每 10 秒轮询前台窗口，匹配 app name/path filename 后调用 `ShortcutManager.blockForGame()`/`unblockAfterGame()`；设置页还维护 `global.gameProcessBlacklist` 的 pill 编辑器和测试。该功能不在 README 主链路中，未上线阶段可先只保留一个全局快捷键开关/空快捷键禁用，删除前台进程轮询、黑名单设置、错误兜底 unblock 和相关测试，等真实用户需要游戏冲突规避再补。
- [ ] 缩小扩展内容脚本的全站补丁面：`apps/extension/manifest.json` 以 `<all_urls>`、`all_frames: true`、`document_start` 注入；`VideoDetector.ts` patch `HTMLMediaElement.prototype.play/pause/load` 和 `Element.prototype.attachShadow`；`DOMObserver.ts` 扫描所有元素与 Shadow DOM；`URLWatcher.ts` patch `history.pushState/replaceState`。商店文案只列 YouTube/Bilibili/Douyin 等受支持站点时，未上线阶段建议先改成 host 白名单和站点适配器，删除全站 Shadow DOM/原型补丁和黑名单兜底，等真正需要“任意视频网站”再扩大权限。
- [ ] 收缩扩展 popup 的重复设置面板：`apps/extension/src/popup.ts` 738 行手写 DOM，配合 `popup.css` 839 行实现状态仪表盘、桌面端点编辑、黑名单编辑、主题/语言设置、媒体卡片和 dashboard port，同类设置在桌面端已有更完整 UI。未上线阶段建议 popup 只保留连接状态、当前媒体和打开桌面设置入口；端点、黑名单、外观/语言统一放桌面端，删除 popup 里的 pill-list 编辑器、重复校验和 dashboard settings 消息。
- [ ] 合并或精简两套自定义 logger：桌面端 `apps/desktop-app/src/main/logger.ts` 实现 console/file transport、异步缓冲、日志轮转、`app.whenReady`/`userData`/`cwd` 多级 fallback、环境变量开关；扩展端 `apps/extension/src/shared/Logger.ts` 又维护分类 allowlist、时间格式和自定义 console 包装。项目未上线时建议先保留薄的 `console` 包装和 `reportError/swallow` 约定，删除文件轮转、cwd fallback、分类 allowlist、双 logger API，等打包诊断确实需要后再加。
- [ ] 暂停 Firefox 双目标兼容层：`apps/extension/esbuild.config.ts` 同时构建 Chrome/Firefox，维护 `manifest.firefox.json`、Firefox MV3 目标和 i18n manifest 测试；README/DEPLOYMENT 也要求两端验证。项目未上线且商店材料当前主打 Chrome Web Store 时，可先只保留 Chromium 目标，删除 Firefox manifest/build script/测试分支，等决定发布 Firefox Add-ons 再补签名和差异验证。
- [ ] 移除默认配置里的 Firefox cookie 硬编码：`DEFAULT_YTDLP_ARGS`、站点 profile 的 `ytDlpArgs`、`DEFAULT_TRANSCRIPTION_YTDLP_ARGS` 都带 `--cookies-from-browser firefox`，但 README 同时支持 Chrome/Edge/Firefox，用户也可能没有 Firefox。未上线阶段建议默认参数不要绑定浏览器 cookie；把 cookie 来源做成显式设置或先不加 cookie 参数，删除测试/占位文案里对 Firefox cookie 的默认假设。
- [ ] 延后跨平台开机启动链路：`DEFAULT_GLOBAL_SETTINGS.autoLaunch` 默认 `true`，`AutoLaunchManager` 同时调用 Windows/macOS `app.setLoginItemSettings` 和 Linux `~/.config/autostart/*.desktop` 写入，并在设置页暴露开关。该能力不在主链路中且预发布默认自启动风险较高；建议先默认关闭，或暂时移除 Linux autostart 文件写入和相关 schema/UI，等安装包策略明确后再实现。
- [ ] 把字幕优先级从用户正则语言收缩成明确选择：`StateManager.pickTrackByPriority()` 对完整 `sourceFile` 执行用户输入正则，`PriorityEditor.vue` 复用可拖拽 pill 编辑器并链接 `docs/subtitle-priority-regex.md`，测试覆盖拖拽、非法正则和高级示例。未上线阶段建议先用语言代码/包含匹配的固定选择（primary/secondary language order），删除正则文档、正则校验、可拖拽排序测试和任意 regex 兜底，等真实字幕源差异证明需要高级匹配再开放。
- [ ] 简化顶部控制面板自动隐藏状态机：`TopControlPanel.vue` 维护 `force-expanded/collapsed/expanded/collapse-pending`、动态测量 header/surface、`blur` 兜底和每 50ms `window.usp.getWindowPointerState()` IPC 轮询；主进程 `windowHandlers.ts` 专门返回鼠标相对窗口坐标，并有大量相关测试。未上线阶段建议默认常驻或仅用前端 `pointerenter/pointerleave`，删除 IPC 鼠标轮询、collapse-pending timer 和几何测量兜底，减少透明无边框窗口上的隐式交互风险。
- [ ] 删除测试脚本里重复的 contracts build 前置：`apps/desktop-app/vitest.config.ts` 和 `apps/extension/vitest.config.ts` 都把 `@immersive-subs/contracts` alias 到 `../../packages/contracts/src/index.ts`，但 root、desktop、extension 的 test scripts 仍多次先跑 `pnpm --filter @immersive-subs/contracts build`。测试运行时既然直接吃源码，就不需要先产出 `packages/contracts/dist`；建议保留 typecheck/build 时的 contracts build，测试脚本改成直接跑各自 vitest，减少无意义的构建链路和 dist 依赖。
